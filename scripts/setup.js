"use strict";
// const { db } = require("@arangodb");
// db._useDatabase("police_register");

// const carsCollection = module.context.collectionName("cars");
// const eventEdges = module.context.collectionName("event");

// if (!db._collection(carsCollection)) {
//   db._createDocumentCollection(carsCollection);
// }

// if (!db._collection(eventEdges)) {
//   db._createEdgeCollection(eventEdges);
// }
